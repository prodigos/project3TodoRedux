import React from "react";

const TodoList = () => {

    // here the todos are going to be displayed

    return(
        <div className="todo-list">

        </div>

    );
};

export default TodoList;
